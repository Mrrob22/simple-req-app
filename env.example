# ===== Core =====
# MongoDB Atlas / локальный Mongo
MONGODB_URI="mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority&appName=<clusterName>"

# Базовый URL приложения
# локально: http://localhost:3000
# прод:    https://<your-vercel-app>.vercel.app
APP_URL="http://localhost:3000"

# ===== Auth (JWT) =====
# Сгенерируй случайные строки (пример: `openssl rand -base64 48`)
JWT_SECRET="<generate_long_random_string>"
# (опционально, если используешь access/refresh-пару)
JWT_REFRESH_SECRET="<another_long_random_string>"

# ===== Bootstrap первого админа =====
# Одноразовый токен для /bootstrap (страница/роут создания первого админа)
BOOTSTRAP_TOKEN="<one_time_token_min_10_chars>"

# ===== Email (SMTP via Nodemailer) =====
# Адрес «от кого» в письмах (обычно совпадает с SMTP_USER)
EMAIL_FROM="no-reply@your-domain.com"

# Использовалось: Ethereal (песочница, письма не уходят наружу; создавай на https://ethereal.email/create)
# SMTP_HOST="smtp.ethereal.email"
# SMTP_PORT="587"
# SMTP_USER="<ethereal_user>"
# SMTP_PASS="<ethereal_pass>"
